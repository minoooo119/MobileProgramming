package edu.skku.map.week5

class ChatRoom(
    val name: String,
    val lastChat: String,
    val thumbnail: Int,
    val groupNumber: Int,
    val lastTime: String,
    val red:Int
) {
    /*
    자바에서의 construct를 대신한다
    kotlin에서는 위에처럼
    */
}